DATABASE = (window.DATABASE || []).concat([
{
    id:"hierarchy-group-simple",
    tags:[ "hierarchy", "group", "moraletriggers" ],
    methods:{
        onMoraleTrigger:{
            EN:" - half the group is eliminated"
        }
    }
},{
    id:"hierarchy-leader-simple",
    tags:[ "hierarchy", "leader", "moraletriggers" ],
    methods:{
        onMoraleTrigger:{
            EN:" - the leader is killed"
        }
    }
}
]);
